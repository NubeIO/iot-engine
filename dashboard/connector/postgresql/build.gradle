ext {
    specifiedLog = project.loggers["hazelcast"] + ["com.github.mauricio": "info"]
}

dependencies {
    compile project(':core:micro')
    compile project(':core:httpserver')
    compile project(':core:cluster:hazelcast')
    compile "io.vertx:vertx-mysql-postgresql-client:$project.versions.vertx"
}
