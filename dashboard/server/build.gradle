ext {
    specifiedLog = project.loggers["sql"] + project.loggers["hazelcast"] + ["org.mongodb.driver": "info"]
}

dependencies {
    compile project(':core:micro')
    compile project(':core:cluster:hazelcast')
    compile project(':core:httpserver')
    compile project(':core:mongo')
    compile "io.vertx:vertx-auth-oauth2:$project.versions.vertx"
}
