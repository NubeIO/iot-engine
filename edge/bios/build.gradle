ext {
    specifiedLog = project.loggers["hazelcast"] + project.loggers["sql"] + ["h2database": "info"]
}

dependencies {
    compile project(':edge:core')
    compile project(':core:cluster:hazelcast')
}
